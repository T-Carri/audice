import { Box } from "@mui/system"

export const StageHistorial = () => {
  return (
    <Box  height={230} style={{ backgroundColor:'grey'}} >
{/* <ul>
 {searchValue&&searchValue.map((item, index) => (
        
          <h5>{item.snippet.title}</h5>
        
      ))} 
</ul>
 */}



{/* 
{snippet.map((e)=>{
    return <h5>e.title</h5>
})} */}

    {/* {searchValue&&searchValue.map(data=>{
         
   
              data.snippet.map((e)=>{
   return <h4>{e.tittle}</h4>
             })  
                 
   
   })}  */}





       </Box>
  )
}
